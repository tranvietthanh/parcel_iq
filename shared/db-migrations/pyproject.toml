[project]
name = "ozpr-db-migrations"
version = "0.1.0"
description = "OZ Property Report database migrations (Alembic)"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.14",
    "psycopg2-binary>=2.9",
]
